# 블록
## 바퀴 속도 설정하기
삐오봇의 바퀴 속도를 설정합니다.  
바퀴 속도가 양수이면 앞쪽 방향으로 회전하고, 바퀴 속도가 음수이면 뒤쪽 방향으로 회전합니다.  
예를 들어, 바퀴 속도가 100 이라면, 앞쪽 방향으로 100의 속도로 회전하고,  
바퀴 속도가 -100 이라면, 뒤쪽 방향으로 100의 속도로 회전합니다.  
한번 바퀴 속도를 설정하면, 다시 바퀴 속도를 설정하기 전까지 해당 속도로 삐오봇이 이동합니다.

![](https://github.com/user-attachments/assets/d7a9b51a-ea89-48f8-9732-c9cbd7de4fab)

### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| wheel | 드롭다운 옵션 | 바퀴 종류 | 왼쪽(left), 오른쪽(right), 양쪽(left, right) |
| velocity | 입력값 | 바퀴 속도 | -100 ~ 100 정수, 0: 정지 | 

### 자바스크립트 코드
```javascript
// 왼쪽 바퀴 속도를 50(으)로 정하기
if($('Pio*0:wheel.move').d != 0) {
    $('Pio*0:wheel.move').d = 0;
}
$('Pio*0:wheel.speed.left').d = __getSpeed('Pio*0', 50);

// 오른쪽 바퀴 속도를 -50(으)로 정하기
if($('Pio*0:wheel.move').d != 0) {
    $('Pio*0:wheel.move').d = 0;
}
$('Pio*0:wheel.speed.right').d = __getSpeed('Pio*0', -50);

// 양쪽 바퀴 속도를 0(으)로 정하기
if($('Pio*0:wheel.move').d != 0) {
    $('Pio*0:wheel.move').d = 0;
}
$('Pio*0:wheel.speed.left').d = __getSpeed('Pio*0', 0);
$('Pio*0:wheel.speed.right').d = __getSpeed('Pio*0', 0);
```

### 파이썬 코드
```python
# 왼쪽 바퀴 속도를 50(으)로 정하기
if __('Pio*0:wheel.move').d != 0:
    __('Pio*0:wheel.move').d = 0
__('Pio*0:wheel.speed.left').d = __getSpeed('Pio*0', 50)

# 오른쪽 바퀴 속도를 -50(으)로 정하기
if __('Pio*0:wheel.move').d != 0:
    __('Pio*0:wheel.move').d = 0
__('Pio*0:wheel.speed.right').d = __getSpeed('Pio*0', -50)

# 양쪽 바퀴 속도를 0(으)로 정하기
if __('Pio*0:wheel.move').d != 0:
    __('Pio*0:wheel.move').d = 0
__('Pio*0:wheel.speed.left').d = __getSpeed('Pio*0', 0)
__('Pio*0:wheel.speed.right').d = __getSpeed('Pio*0', 0)
```

## 거리 이동하기
삐오봇이 이동할 거리를 설정합니다.  
바퀴 속도가 설정되어 있지 않은 경우, 이동하지 않습니다.  
거리 값이 0일 경우에는, 현재 설정되어 있는 바퀴 속도대로 멈추지 않고 이동합니다.  
기다리기를 체크하면, 이동이 완료될 때까지 기다립니다.  
단, 기다리기를 체크한 경우에는 async 함수 내에서만 사용할 수 있습니다.

![](https://github.com/user-attachments/assets/8a2dedfc-c001-4199-ace3-43f39dee09f0)

### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| distance | 입력값 | 거리 값 | 0 이상 실수 |
| unit | 드롭다운 옵션 | 거리 단위 | cm, mm, 인치(inch) | 

### 자바스크립트 코드
```javascript
// 50 cm 이동하기 | 기다리기 O
$('Pio*0:wheel.move').d = __getDistance('Pio*0', 50, 'cm'); // (robot, distance, unit)
await $('Pio*0:wheel.!move').w();

// 50 mm 이동하기 | 기다리기 X
$('Pio*0:wheel.move').d = __getDistance('Pio*0', 50, 'mm'); // (robot, distance, unit)

// 50 inch 이동하기 | 기다리기 X
$('Pio*0:wheel.move').d = __getDistance('Pio*0', 50, 'inch'); // (robot, distance, unit)
```

### 파이썬 코드
```python
# 50 cm 이동하기 | 기다리기 O
__('Pio*0:wheel.move').d = __getDistance('Pio*0', 50, 'cm') # (robot, distance, unit)
await __('Pio*0:wheel.!move').w()

# 50 mm 이동하기 | 기다리기 X
__('Pio*0:wheel.move').d = __getDistance('Pio*0', 50, 'mm') # (robot, distance, unit)

# 50 inch 이동하기 | 기다리기 X
__('Pio*0:wheel.move').d = __getDistance('Pio*0', 50, 'inch') # (robot, distance, unit)
```

## 시간 이동하기
삐오봇이 이동할 시간을 설정합니다.  
바퀴 속도가 설정되어 있지 않은 경우, 이동하지 않습니다.    
기다리기를 체크하면, 이동이 완료될 때까지 기다립니다.  
단, 기다리기를 체크한 경우에는 async 함수 내에서만 사용할 수 있습니다.  
  
![](https://github.com/user-attachments/assets/a1a3414f-27fe-4307-b18f-d40e38ba3d1e)

### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| time | 입력값 | 시간 값 | 0 이상 실수 |
| unit | 드롭다운 옵션 | 시간 단위 | 초(seconds), 밀리초(milliseconds) | 

옵션을 밀리초(milliseconds)로 설정한 경우에는, time 값을 1000으로 나눈 값이 입력됩니다.

### 자바스크립트 코드
```javascript
// 5 초 이동하기 | 기다리기 O
await __stopAfterDelay('Pio*0', 5, true); // (robot, time, wait_w)

// 5 밀리초 이동하기 | 기다리기 X
__stopAfterDelay('Pio*0', 0.005, false); // (robot, time, wait_w)
``` 

### 파이썬 코드
```python
# 5 초 이동하기 | 기다리기 O
await __stopAfterDelay('Pio*0', 5, True) # (robot, time, wait_w)

# 5 밀리초 이동하기 | 기다리기 X
__stopAfterDelay('Pio*0', 0.005, False) # (robot, time, wait_w)
```

## 제자리 돌기
삐오봇이 제자리에서 회전할 방향과 각도를 설정합니다.  
기다리기를 체크하면, 이동이 완료될 때까지 기다립니다.  
단, 기다리기를 체크한 경우에는 async 함수 내에서만 사용할 수 있습니다.  
  
![](https://github.com/user-attachments/assets/0db0bfa9-d0f8-4478-b1de-af29e317abca)

### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| direction | 드롭다운 옵션 | 회전 방향 | 왼쪽(left), 오른쪽(right) |
| angle | 입력값 | 회전 각도 | 0 이상 정수 |

### 자바스크립트 코드
```javascript
// 왼쪽으로 90도 제자리 돌기 | 기다리기 O
await __turn_degree_left('Pio*0', 90, true); // (robot, degree, wait_w)

// 오른쪽으로 90도 제자리 돌기 | 기다리기 X
__turn_degree_right('Pio*0', 90, false); // (robot, degree, wait_w)
```

### 파이썬 코드
```python
# 왼쪽으로 90도 제자리 돌기 | 기다리기 O
await __turn_degree_left('Pio*0', 90, True) # (robot, degree, wait_w)

# 오른쪽으로 90도 제자리 돌기 | 기다리기 X
__turn_degree_right('Pio*0', 90, False) # (robot, degree, wait_w)
```

## 바퀴 속도 변경하기
삐오봇의 바퀴 속도를 변경합니다.  
현재의 바퀴 속도에 입력한 속도를 더한 값이 새로운 바퀴 속도가 됩니다.  
새롭게 설정된 바퀴 속도의 범위는 -100 ~ 100으로 설정됩니다.

![](https://github.com/user-attachments/assets/c80ac666-5d51-4e24-a54f-9c4717433b9f)

### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| wheel | 드롭다운 옵션 | 바퀴 종류 | 왼쪽(left), 오른쪽(right), 양쪽(left, right) |
| velocity | 입력값 | 현재 바퀴 속도에 더할 속도 값 | -200 ~ 200 정수, 0: 정지 | 
 
### 자바스크립트 코드
```javascript
// 왼쪽 바퀴 속도를 50만큼 바꾸기
if($('Pio*0:wheel.move').d != 0) {
    $('Pio*0:wheel.move').d = 0;
}
$('Pio*0:wheel.speed.left').d = $('Pio*0:wheel.speed.left').d + __getSpeed('Pio*0', 50); // (robot, value)

// 오른쪽 바퀴 속도를 50만큼 바꾸기
if($('Pio*0:wheel.move').d != 0) {
    $('Pio*0:wheel.move').d = 0;
}
$('Pio*0:wheel.speed.right').d = $('Pio*0:wheel.speed.right').d + __getSpeed('Pio*0', 50); // (robot, value)

// 양쪽 바퀴 속도를 50만큼 바꾸기
if($('Pio*0:wheel.move').d != 0) {
    $('Pio*0:wheel.move').d = 0;
}
$('Pio*0:wheel.speed.left').d = $('Pio*0:wheel.speed.left').d + __getSpeed('Pio*0', 50); // (robot, value)
$('Pio*0:wheel.speed.right').d = $('Pio*0:wheel.speed.right').d + __getSpeed('Pio*0', 50); // (robot, value)
```

### 파이썬 코드
```python
# 왼쪽 바퀴 속도를 50만큼 바꾸기
if __('Pio*0:wheel.move').d != 0:
    __('Pio*0:wheel.move').d = 0
__('Pio*0:wheel.speed.left').d = __('Pio*0:wheel.speed.left').d + __getSpeed('Pio*0', 50) # (robot, value)

# 오른쪽 바퀴 속도를 50만큼 바꾸기
if __('Pio*0:wheel.move').d != 0:
    __('Pio*0:wheel.move').d = 0
__('Pio*0:wheel.speed.right').d = __('Pio*0:wheel.speed.right').d + __getSpeed('Pio*0', 50) # (robot, value)

# 양쪽 바퀴 속도를 50만큼 바꾸기
if __('Pio*0:wheel.move').d != 0:
    __('Pio*0:wheel.move').d = 0
__('Pio*0:wheel.speed.left').d = __('Pio*0:wheel.speed.left').d + __getSpeed('Pio*0', 50) # (robot, value)
__('Pio*0:wheel.speed.right').d = __('Pio*0:wheel.speed.right').d + __getSpeed('Pio*0', 50) # (robot, value)
```

## 터보 모드 켜기 / 끄기
삐오봇의 터보모드를 켜거나 끕니다.

![](https://github.com/user-attachments/assets/e908d5fc-4723-4927-81c2-6e56829bed1e)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|toggle|드롭다운 옵션|터보 토글|켜기(true), 끄기(false)|

### 자바스크립트 코드
```javascript
// 터보 모드 켜기
__turbo('Pio*0', true);

// 터보 모드 끄기
__turbo('Pio*0', false);
```

### 파이썬 코드
```python
# 터보 모드 켜기
__turbo('Pio*0', True)

# 터보 모드 끄기
__turbo('Pio*0', False)
```

## 정지하기
삐오봇의 이동을 멈춥니다.  
삐오봇의 양쪽 바퀴 속도가 모두 0으로 초기화됩니다.

![](https://github.com/user-attachments/assets/3e74c47f-e15c-4346-be0d-5dc9b9ffde95)

 
### 자바스크립트 코드
```javascript
// 삐오봇 정지하기
__stopMove('Pio*0');
```

### 파이썬 코드
```python
# 삐오봇 정지하기
__stopMove('Pio*0')
```

## 바퀴가 움직이는 중인가?
삐오봇의 바퀴가 움직이고 있는지 아닌지 여부를 **참(1) / 거짓(0)** (으)로 반환합니다.

![](https://github.com/user-attachments/assets/c98e9562-a8d9-4a8a-9b00-bc5c649c27e5)

 
### 자바스크립트 코드
```javascript
$('Pio*0:wheel.moving').d;
```

### 파이썬 코드
```python
__('Pio*0:wheel.moving').d
```

## 말판 앞으로 한 칸 이동하기
삐오봇이 말판 위에서 한 칸 이동합니다.  
기다리기를 체크하면, 이동이 완료될 때까지 기다립니다.  
단, 기다리기를 체크한 경우에는 async 함수 내에서만 사용할 수 있습니다.  
  
![](https://github.com/user-attachments/assets/96ae3aa2-784f-4ec2-b4b7-f9e65bb081a0)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| direction | 드롭다운 옵션 | 이동 방향 | 앞으로(forward), 뒤로(backward), 왼쪽으로(left), 오른쪽으로(right)|

### 자바스크립트 코드
```javascript
// 말판 앞으로 한 칸 이동하기 | 기다리기 O
await __grid_move_forward('Pio*0', true); // (robot, wait_w)

// 말판 뒤로 한 칸 이동하기 | 기다리기 O
await __grid_move_backward('Pio*0', true); // (robot, wait_w)

// 말판 왼쪽으로 한 칸 이동하기 | 기다리기 X
__grid_move_left('Pio*0', false); // (robot, wait_w)

// 말판 오른쪽으로 한 칸 이동하기 | 기다리기 X
__grid_move_right('Pio*0', false); // (robot, wait_w)
```

### 파이썬 코드
```python
# 말판 앞으로 한 칸 이동하기 | 기다리기 O
await __grid_move_forward('Pio*0', True) # (robot, wait_w)

# 말판 뒤로 한 칸 이동하기 | 기다리기 O
await __grid_move_backward('Pio*0', True) # (robot, wait_w)

# 말판 왼쪽으로 한 칸 이동하기 | 기다리기 X
__grid_move_left('Pio*0', False) # (robot, wait_w)

# 말판 오른쪽으로 한 칸 이동하기 | 기다리기 X
__grid_move_right('Pio*0', False) # (robot, wait_w)
```

## 말판에서 한번 돌기
말판 위 삐오봇이 입력받은 방향으로 90도 회전합니다.  
기다리기를 체크하면, 이동이 완료될 때까지 기다립니다.  
단, 기다리기를 체크한 경우에는 async 함수 내에서만 사용할 수 있습니다.  
  
![](https://github.com/user-attachments/assets/7a8da23b-c598-4743-939d-16a1056ccd17)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| direction | 드롭다운 옵션 | 회전 방향 | 왼쪽(left), 오른쪽(right)|

### 자바스크립트 코드
```javascript
// 말판 위에서 왼쪽으로 한번 돌기 | 기다리기 O
await __grid_turn_left('Pio*0', true); // (robot, wait_w)

// 말판 위에서 오른쪽으로 한번 돌기 | 기다리기 X
__grid_turn_right('Pio*0', false); // (robot, wait_w)

```

### 파이썬 코드
```python
# 말판 위에서 왼쪽으로 한번 돌기 | 기다리기 O
await __grid_turn_left('Pio*0', True) # (robot, wait_w)

# 말판 위에서 오른쪽으로 한번 돌기 | 기다리기 X
__grid_turn_right('Pio*0', False) # (robot, wait_w)
```

## 목 회전속도 설정하기
삐오봇 목의 회전속도를 설정합니다.  
목 속도의 범위는 1 ~ 6 입니다.

![](https://github.com/user-attachments/assets/520b3466-2c6f-4216-8226-f8e94a423f29)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|speed|입력값|목 회전속도| 1 ~ 6 사이 실수|
### 자바스크립트 코드
```javascript
// 삐오봇 목 회전속도를 4로 정하기
$('Pio*0:neck.speed').d = 4;
```

### 파이썬 코드
```python
# 삐오봇 목 회전속도를 4로 정하기
__('Pio*0:neck.speed').d = 4
```

## 목 각도 설정하기
삐오봇 목을 회전하여 도착할 각도를 설정합니다.  
목 각도의 범위는 -45 ~ 45도 입니다.

![](https://github.com/user-attachments/assets/37017f1f-b6fe-44da-aad3-1a5d365a498f)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|angle|입력값|목 각도| -45 ~ 45 사이 실수|
### 자바스크립트 코드
```javascript
// 삐오봇 목 각도를 10 도로 바꾸기 | 기다리기 O
$('Pio*0:neck.angle').d = 10;
await $('Pio*0:neck.!angle').w();

// 삐오봇 목 각도를 -10 도로 바꾸기 | 기다리기 X
$('Pio*0:neck.angle').d = -10;
```

### 파이썬 코드
```python
# 삐오봇 목 각도를 10 도로 바꾸기 | 기다리기 O
__('Pio*0:neck.angle').d = 10
await __('Pio*0:neck.!angle').w()

# 삐오봇 목 각도를 -10 도로 바꾸기 | 기다리기 X
__('Pio*0:neck.angle').d = -10
```

## 목이 움직이는 중인가?
삐오봇의 목이 움직이고 있는지 아닌지 여부를 **참(1) / 거짓(0)** (으)로 반환합니다.

![](https://github.com/user-attachments/assets/4dfdf859-9046-4622-9079-e6bdac7fcf62)
 


### 자바스크립트 코드
```javascript
$('Pio*0:neck.moving').d;
```

### 파이썬 코드
```python
__('Pio*0:neck.moving').d
```

## 눈 LED 색상 설정하기
삐오봇의 눈 LED 색을 변경합니다.  
왼쪽, 오른쪽 또는 양쪽의 눈 LED 색을 변경할 수 있습니다.


![](https://github.com/user-attachments/assets/2f059c98-9804-4a2e-9847-9e9255ac4c31)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| direction | 드롭다운 옵션 | 눈 방향 | 왼쪽(left), 오른쪽(right), 양쪽(left, right) |
| color | 드롭다운 옵션 | 색상 | 검정색([0, 0, 0]), 빨간색([255, 0, 0]), 노란색([255, 255, 0]), 초록색([0, 255, 0]), 청록색([0, 255, 255]), 파란색([0, 0, 255]), 자홍색([255, 0, 255]), 흰색([255, 255, 255])|

### 자바스크립트 코드
```javascript
// 삐오봇 왼쪽눈을 검정색으로 정하기
$('Pio*0:eye.left.rgb').d = [0, 0, 0];

// 삐오봇 오른쪽 눈을 빨간색으로 정하기
$('Pio*0:eye.right.rgb').d = [255, 0, 0];

// 삐오봇 양쪽 눈을 노란색으로 정하기
$('Pio*0:eye.left.rgb').d = [255, 255, 0];
$('Pio*0:eye.right.rgb').d = [255, 255, 0];
```

### 파이썬 코드
```python
# 삐오봇 왼쪽 눈을 검정색으로 정하기
__('Pio*0:eye.left.rgb').d = [0, 0, 0]

# 삐오봇 오른쪽 눈을 빨간색으로 정하기
__('Pio*0:eye.right.rgb').d = [255, 0, 0]

# 삐오봇 양쪽 눈을 노란색으로 정하기
__('Pio*0:eye.left.rgb').d = [255, 255, 0]
__('Pio*0:eye.right.rgb').d = [255, 255, 0]
```

## 눈 LED 색 색상 카테고리 블록으로 설정하기
삐오봇의 눈 LED 색을 색상 카테고리 블록으로 설정합니다.  
왼쪽, 오른쪽 또는 양쪽의 눈 LED 색을 변경할 수 있습니다.


![](https://github.com/user-attachments/assets/740618fd-7e74-4a4d-bc7f-f15136682566)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| direction | 드롭다운 옵션 | 적용 LED 방향 | 왼쪽(left), 오른쪽(right), 양쪽(left, right)|
| color | 입력값 | 눈 LED 색상 | RGB 배열([255,255,255]) | 

### 자바스크립트 코드
```javascript
// 삐오봇 왼쪽 눈을 빨간색으로 정하기
$('Pio*0:eye.left.rgb').d = [255, 0, 0];

// 삐오봇 오른쪽 눈을 R:255, G:255, B:255로 정하기
$('Pio*0:eye.right.rgb').d = [255, 255, 255];

// 삐오봇 양쪽 눈을 무작위 색상으로 정하기
$('Pio*0:eye.left.rgb').d = __randomColor();
$('Pio*0:eye.right.rgb').d = __randomColor();
```

### 파이썬 코드
```python
# 삐오봇 왼쪽 눈을 빨간색으로 정하기
__('Pio*0:eye.left.rgb').d = [255, 0, 0]

# 삐오봇 오른쪽 눈을 R:255, G:255, B:255로 정하기
__('Pio*0:eye.right.rgb').d = [255, 255, 255]

# 삐오봇 양쪽 눈을 무작위 색상으로 정하기
__('Pio*0:eye.left.rgb').d = __randomColor()
__('Pio*0:eye.right.rgb').d = __randomColor()
```


## 눈 LED 색 지정 RGB만큼 변경하기
지정한 R,G,B 값만큼 삐오봇의 눈 LED 색을 변경합니다.   
왼쪽, 오른쪽 또는 양쪽의 색을 설정할 수 있습니다.


![](https://github.com/user-attachments/assets/840683e7-6480-47f6-9b92-e66e34eecb53)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| direction | 드롭다운 옵션 | 적용 LED 방향 | 왼쪽(left), 오른쪽(right), 양쪽(left, right)|
| color | 입력값 | 변경 R,G,B 값 | R,G,B 각각 -255~255 사이 정수 | 

### 자바스크립트 코드
```javascript
// 삐오봇의 왼쪽 눈을 R : 1, G : 2, B : 3 만큼 바꾸기
$('Pio*0:eye.left.rgb').d = [$('Pio*0:eye.left.rgb').d[0] + 1, $('Pio*0:eye.left.rgb').d[1] + 2, $('Pio*0:eye.left.rgb').d[2] + 3];

// 삐오봇의 오른쪽 눈을 R : -1, G : -2, B : -3 만큼 바꾸기
$('Pio*0:eye.right.rgb').d = [$('Pio*0:eye.right.rgb').d[0] + -1, $('Pio*0:eye.right.rgb').d[1] + -2, $('Pio*0:eye.right.rgb').d[2] + -3];

// 삐오봇의 양쪽 눈을 R : 10, G : 20, B : 30 만큼 바꾸기
$('Pio*0:eye.left.rgb').d = [$('Pio*0:eye.left.rgb').d[0] + 10, $('Pio*0:eye.left.rgb').d[1] + 20, $('Pio*0:eye.left.rgb').d[2] + 30];
$('Pio*0:eye.right.rgb').d = [$('Pio*0:eye.right.rgb').d[0] + 10, $('Pio*0:eye.right.rgb').d[1] + 20, $('Pio*0:eye.right.rgb').d[2] + 30];

```

### 파이썬 코드
```python
# 삐오봇의 왼쪽 눈을 R : 1, G : 2, B : 3 만큼 바꾸기
__('Pio*0:eye.left.rgb').d = [__('Pio*0:eye.left.rgb').d[0] + 1, __('Pio*0:eye.left.rgb').d[1] + 2, __('Pio*0:eye.left.rgb').d[2] + 3]

# 삐오봇의 오른쪽 눈을 R : -1, G : -2, B : -3 만큼 바꾸기
__('Pio*0:eye.right.rgb').d = [__('Pio*0:eye.right.rgb').d[0] + -1, __('Pio*0:eye.right.rgb').d[1] + -2, __('Pio*0:eye.right.rgb').d[2] + -3]

# 삐오봇의 양쪽 눈을 R : 10, G : 20, B : 30 만큼 바꾸기
__('Pio*0:eye.left.rgb').d = [__('Pio*0:eye.left.rgb').d[0] + 10, __('Pio*0:eye.left.rgb').d[1] + 20, __('Pio*0:eye.left.rgb').d[2] + 30]
__('Pio*0:eye.right.rgb').d = [__('Pio*0:eye.right.rgb').d[0] + 10, __('Pio*0:eye.right.rgb').d[1] + 20, __('Pio*0:eye.right.rgb').d[2] + 30]
```


## 눈 패턴 변경하기
삐오봇의 눈 패턴을 변경합니다.  
패턴을 정하고 패턴 시작시 눈의 색상을 각각 정할 수 있습니다.  
단, 눈 패턴을 무지개로 선택시엔 눈의 색상을 기본, 노란색, 청록색, 자홍색 중 하나로만 선택 가능합니다.


![](https://github.com/user-attachments/assets/f46c0e4c-03b8-42ff-8d36-3db3aafc9c3c)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|pattern|드롭다운 옵션|눈 패턴|끄기(0), 깜빡이기(1), 디밍(2), 무지개(3)|
|color|드롭다운 옵션|왼쪽, 오른쪽 눈 LED 색상|빨간색(1), 노란색(2), 초록색(3), 청록색(4), 파란색(5), 자홍색(6), 흰색(7) |

### 자바스크립트 코드
```javascript
// 삐오봇의 눈 패턴을 깜빡이기로 정하기 | 왼쪽 눈 : 빨간색 오른쪽 눈 : 노란색
$('Pio*0:eye.pattern.mode').d = 1;
$('Pio*0:eye.pattern.parameter').d = [1,0,0,2,0,0];

// 삐오봇의 눈 패턴을 디밍으로 정하기 | 왼쪽 눈 : 초록색 오른쪽 눈 : 파란색
$('Pio*0:eye.pattern.mode').d = 2;
$('Pio*0:eye.pattern.parameter').d = [3,0,0,5,0,0];

// 삐오봇의 눈 패턴을 무지개로 정하기 | 왼쪽 눈 : 청록색 오른쪽 눈 : 자홍색
$('Pio*0:eye.pattern.mode').d = 3;
$('Pio*0:eye.pattern.parameter').d = [3,0,0,5,0,0];
```

### 파이썬 코드
```python
# 삐오봇의 눈 패턴을 깜빡이기로 정하기 | 왼쪽 눈 : 빨간색 오른쪽 눈 : 노란색
__('Pio*0:eye.pattern.mode').d = 1
__('Pio*0:eye.pattern.parameter').d = [1,0,0,2,0,0]

# 삐오봇의 눈 패턴을 디밍으로 정하기 | 왼쪽 눈 : 초록색 오른쪽 눈 : 파란색
__('Pio*0:eye.pattern.mode').d = 2
__('Pio*0:eye.pattern.parameter').d = [3,0,0,5,0,0]

# 삐오봇의 눈 패턴을 무지개로 정하기 | 왼쪽 눈 : 청록색 오른쪽 눈 : 자홍색
__('Pio*0:eye.pattern.mode').d = 3
__('Pio*0:eye.pattern.parameter').d = [3,0,0,5,0,0]
```

## LED 끄기
삐오봇의 눈 LED 색을 없앱니다.  
왼쪽, 오른쪽 또는 양쪽의 눈 LED 색을 없앨 수 있습니다.

![](https://github.com/user-attachments/assets/b950b755-a26f-4ece-a588-b6ff2b3804e9)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| direction | 드롭다운 옵션 | 적용 LED 방향 | 왼쪽(left), 오른쪽(right), 양쪽(left, right)|

### 자바스크립트 코드
```javascript
// 삐오봇 왼쪽 눈 끄기
$('Pio*0:eye.left.rgb').d = [0, 0, 0];

// 삐오봇 오른쪽 눈 끄기
$('Pio*0:eye.right.rgb').d = [0, 0, 0];

// 삐오봇 양쪽 눈 끄기
$('Pio*0:eye.left.rgb').d = [0, 0, 0];
$('Pio*0:eye.right.rgb').d = [0, 0, 0];
```

### 파이썬 코드
```python
# 삐오봇 왼쪽 눈 끄기
__('Pio*0:eye.left.rgb').d = [0, 0, 0]

# 삐오봇 오른쪽 눈 끄기
__('Pio*0:eye.right.rgb').d = [0, 0, 0]

# 삐오봇 양쪽 눈 끄기
__('Pio*0:eye.left.rgb').d = [0, 0, 0]
__('Pio*0:eye.right.rgb').d = [0, 0, 0]
```

## 버저음 설정하기
지정된 주파수로 삐오봇의 버저음을 설정합니다.  
소리낼 수 있는 주파수의 범위는 27.5hz ~ 6553.5hz 입니다.  
이 외의 값을 입력하면 버저음이 발생하지 않습니다.

![](https://github.com/user-attachments/assets/4f044988-c3c6-4af2-8882-852f855f1c55)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| sound | 입력값 | 버저음 주파수 | 27.5 ~ 6553.5(hz)|

### 자바스크립트 코드
```javascript
// 버저음 주파수 1000hz로 설정하기
__stopSound('Pio*0');
$('Pio*0:sound.buzz').d = 1000;
```

### 파이썬 코드
```python
# 버저음 주파수 1000hz로 설정하기
__stopSound('Pio*0')
__('Pio*0:sound.buzz').d = 1000
```

## 음계 연주하기
삐오봇이 지정된 음계를 재생합니다.

![](https://github.com/user-attachments/assets/94ecec23-e49e-49c0-97ad-e4d124429124)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| note | 드롭다운 옵션 | 음계 | 도(Do), 도#(Do#), 레(Re), 레#(Re#), 미(Mi), 파(Fa), 파#(Fa#), 솔(So), 솔#(So#), 라(La), 라#(La#), 시(Ti)|
| octave | 드롭다운 옵션 | 옥타브 | 1 ~ 7 |

### 자바스크립트 코드
```javascript
// 1 옥타브 도(Do) 음을 연주하기
__stopSound('Pio*0');
$('Pio*0:sound.note').d = 4;

// 1 옥타브 레(Re) 음을 연주하기
__stopSound('Pio*0');
$('Pio*0:sound.note').d = 6;

// 2 옥타브 도(Do) 음을 연주하기
__stopSound('Pio*0');
$('Pio*0:sound.note').d = 16;

// 7 옥타브 시(Ti) 음을 연주하기
__stopSound('Pio*0');
$('Pio*0:sound.note').d = 87;
```

### 파이썬 코드
```python
# 1 옥타브 도(Do) 음을 연주하기
__stopSound('Pio*0')
__('Pio*0:sound.note').d = 4

# 1 옥타브 레(Re) 음을 연주하기
__stopSound('Pio*0')
__('Pio*0:sound.note').d = 6

# 2 옥타브 도(Do) 음을 연주하기
__stopSound('Pio*0')
__('Pio*0:sound.note').d = 16

# 7 옥타브 시(Ti) 음을 연주하기
__stopSound('Pio*0')
__('Pio*0:sound.note').d = 87
```


## 소리 재생하기
삐오봇이 특정 사운드 클립을 재생합니다.  
기다리기를 체크하면, 재생이 완료될 때까지 기다립니다.  
단, 기다리기를 체크한 경우에는 async 함수 내에서만 사용할 수 있습니다.  

![](https://github.com/user-attachments/assets/fe461d8b-875e-4bb9-8d0c-42181adf936e)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| sound_clip | 드롭다운 옵션 | 사운드 클립 | beep(1), beep2(2), beep3(3), beep_repeat(4), beep_random(5), beep_random_repeat(6), snore(7), snore_repeat(8), siren(9), siren_repeat(10), engine(11), engine_repeat(12), fart_a(13), fart_b(14), noise(15), noise_repeat(16), whistle(17), chop(18), chop_repeat(19), melody(20), robot_r2d2(21), connect(22), happy(81)|

### 자바스크립트 코드
```javascript
// beep(1) 소리 재생하기 | 기다리기 O
__stopSound('Pio*0');
$('Pio*0:sound.clip').d = 1;
await $('Pio*0:sound.!clip').w();

// happy(81) 소리 재생하기 | 기다리기 X
__stopSound('Pio*0');
$('Pio*0:sound.clip').d = 81;
```

### 파이썬 코드
```python
# beep(1) 소리 재생하기 | 기다리기 O
__stopSound('Pio*0')
__('Pio*0:sound.clip').d = 1
await __('Pio*0:sound.!clip').w()

# happy(81) 소리 재생하기 | 기다리기 X
__stopSound('Pio*0')
__('Pio*0:sound.clip').d = 81
```



## 멜로디 재생하기
삐오봇이 특정 멜로디를 재생합니다.  
기다리기를 체크하면, 재생이 완료될 때까지 기다립니다.  
단, 기다리기를 체크한 경우에는 async 함수 내에서만 사용할 수 있습니다.  

![](https://github.com/user-attachments/assets/c18c9202-523b-4732-bed0-6e6f47b1c34b)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| melody | 드롭다운 옵션 | 멜로디 | happy(1), angry(2), sad(3), sleep(4), march(5), birthday(6), dee_bee(7), finish(8), toy_poop(9), toy_bath(10), toy_sad(11), toy_happy(12), toy_angry(13), toy_sleep(14) |

### 자바스크립트 코드
```javascript
// toy_happy(12) 멜로디 재생하기 | 기다리기 O
__stopSound('Pio*0');
$('Pio*0:sound.melody').d = 12;
await $('Pio*0:sound.!melody').w();

// sleep(4) 멜로디 재생하기 | 기다리기 X
__stopSound('Pio*0');
$('Pio*0:sound.melody').d = 4;
```

### 파이썬 코드
```python
# toy_happy(12) 멜로디 재생하기 | 기다리기 O
__stopSound('Pio*0')
__('Pio*0:sound.melody').d = 12
await __('Pio*0:sound.!melody').w()

# sleep(4) 멜로디 재생하기 | 기다리기 X
__stopSound('Pio*0')
__('Pio*0:sound.melody').d = 4
```

## 소리 끄기
삐오봇의 소리를 끕니다.

![](https://github.com/user-attachments/assets/463feecb-aadc-495a-9035-d0b30fc02377)


### 자바스크립트 코드
```javascript
// 삐오봇 소리 끄기
__stopSound('Pio*0');
```

### 파이썬 코드
```python
# 삐오봇 소리 끄기
__stopSound('Pio*0')
```

## 소리가 재생 중인가?
삐오봇의 소리가 재생중인지 아닌지 여부를 **참(1) / 거짓(0)** (으)로 반환합니다.

![](https://github.com/user-attachments/assets/f34d5037-4f51-4ec6-ae52-8f99e945922d)


### 자바스크립트 코드
```javascript
// 삐오봇의 소리가 재생 중인가? - 재생시 true, 아닐시 false
$('Pio*0:sound.playing').d;
```

### 파이썬 코드
```python
# 삐오봇의 소리가 재생 중인가? - 재생시 True, 아닐시 False
__('Pio*0:sound.playing').d
```

## 바퀴 속도 값
삐오봇의 지정한 바퀴 속도 값을 반환합니다.

![](https://github.com/user-attachments/assets/481872f2-d45d-484b-87d8-bc25da34ddbc)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
| direction | 드롭다운 옵션 | 방향 | 왼쪽(left), 오른쪽(right) |
### 자바스크립트 코드
```javascript
//왼쪽 바퀴 속도
__getSpeedInput('Pio*0', $('Pio*0:wheel.speed.left').d);

//오른쪽 바퀴 속도
__getSpeedInput('Pio*0', $('Pio*0:wheel.speed.right').d);

```

### 파이썬 코드
```python
# 왼쪽 바퀴 속도
__getSpeedInput('Pio*0', __('Pio*0:wheel.speed.left').d)

# 오른쪽 바퀴 속도
__getSpeedInput('Pio*0', __('Pio*0:wheel.speed.right').d)
```

## 신호 세기 값
삐오봇의 신호 세기 값을 반환합니다.  
로봇과 동글 사이의 신호 세기를 나타내며, 거리가 가까울수록 값이 커집니다.  
안정적인 동작을 위해 `-50dBm ~ -80dBm` 범위 내에서 사용 권장합니다.  
신호 세기 값은 0에 가까울수록(예: -40 > -90) 신호가 강한 상태입니다. 

![](https://github.com/user-attachments/assets/0373b4c0-f3cb-4409-b3ab-02989ee76871)


### 자바스크립트 코드
```javascript
// 신호 세기 값
$('Pio*0:signal_strength').d;
```

### 파이썬 코드
```python
# 신호 세기 값
__('Pio*0:signal_strength').d
```

## 배터리 전압
삐오봇의 배터리 전압 값을 반환합니다.  
배터리 완충 시 전압은 약 `4.0V`이며, 보통 `3.7V` 이상에서 안정적으로 동작합니다.  
전압이 `3.6V` 이하로 떨어지면 동작 오류가 발생할 수 있으므로 반드시 충전을 권장합니다.

![](https://github.com/user-attachments/assets/5ff9b826-160d-44f6-8acb-127156f4cff0)

### 자바스크립트 코드
```javascript
// 배터리 전압 값
$('Pio*0:battery.level').d;
```

### 파이썬 코드
```python
# 배터리 전압 값
__('Pio*0:battery.level').d
```

## 마지막으로 누른 버튼이 ~ 인가?
사용자가 마지막으로 누른 버튼에 따라 **참(1) / 거짓(0)** (으)로 반환합니다.

![](https://github.com/user-attachments/assets/425b73fb-72e7-4ecc-ab96-eac443835848)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|condition|드롭다운 옵션|마지막으로 누른 버튼|앞으로 가기(2), 뒤로 가기(4), 왼쪽으로 가기(8), 오른쪽으로 가기(16), 실행하기(1), 행동하기(32), 반복하기(64), 삭제하기(128)|
### 자바스크립트 코드
```javascript
// 삐오봇 앞으로 가기(2) 버튼을 눌렀는가?
$('Pio*0:keypad').e && $('Pio*0:keypad').d == 2

// 삐오봇 뒤로 가기(4) 버튼을 눌렀는가?
$('Pio*0:keypad').e && $('Pio*0:keypad').d == 4

// 삐오봇 왼쪽으로(8) 가기 버튼을 눌렀는가?
$('Pio*0:keypad').e && $('Pio*0:keypad').d == 8

// 삐오봇 오른쪽으로(16) 가기 버튼을 눌렀는가?
$('Pio*0:keypad').e && $('Pio*0:keypad').d == 16

// 삐오봇 실행하기(1) 버튼을 눌렀는가?
$('Pio*0:keypad').e && $('Pio*0:keypad').d == 1

// 삐오봇 행동하기(32) 버튼을 눌렀는가?
$('Pio*0:keypad').e && $('Pio*0:keypad').d == 32

// 삐오봇 반복하기(64) 버튼을 눌렀는가?
$('Pio*0:keypad').e && $('Pio*0:keypad').d == 64

// 삐오봇 삭제하기(128) 버튼을 눌렀는가?
$('Pio*0:keypad').e && $('Pio*0:keypad').d == 128

```

### 파이썬 코드
```python
# 삐오봇 앞으로 가기(2) 버튼을 눌렀는가?
__('Pio*0:keypad').e and __('Pio*0:keypad').d == 2

# 삐오봇 뒤로 가기(4) 버튼을 눌렀는가?
__('Pio*0:keypad').e and __('Pio*0:keypad').d == 4

# 삐오봇 왼쪽으로(8) 가기 버튼을 눌렀는가?
__('Pio*0:keypad').e and __('Pio*0:keypad').d == 8

# 삐오봇 오른쪽으로(16) 가기 버튼을 눌렀는가?
__('Pio*0:keypad').e and __('Pio*0:keypad').d == 16

# 삐오봇 실행하기(1) 버튼을 눌렀는가?
__('Pio*0:keypad').e and __('Pio*0:keypad').d == 1

# 삐오봇 행동하기(32) 버튼을 눌렀는가?
__('Pio*0:keypad').e and __('Pio*0:keypad').d == 32

# 삐오봇 반복하기(64) 버튼을 눌렀는가?
__('Pio*0:keypad').e and __('Pio*0:keypad').d == 64

# 삐오봇 삭제하기(128) 버튼을 눌렀는가?
__('Pio*0:keypad').e and __('Pio*0:keypad').d == 128
```
