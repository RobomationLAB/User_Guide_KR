# 대시보드 열기
**대시보드 열기** 는 블록 코딩에 사용할 수 있는 블록은 아니지만,  
확장 모듈에서 사용되는 모델이 어떠한 방식으로 적용되는지 확인하는 대시보드를 열 수 있습니다.

## 대시보드 화면
대시보드 열기 클릭 시 다음과 같은 화면을 볼 수 있습니다.  
  
  
![](https://github.com/user-attachments/assets/5f57c558-0e1c-4804-ad34-125125ff7edc)


## 세부 기능

### Power
선택한 카메라를 키거나 끕니다.

### Load Model 
학습된 나이, 성별, 표정 모델을 불러옵니다. '나이, 성별, 표정' 확장 모듈을 사용하기 위해서 반드시 필요한 작업입니다.

### Show Result

나이, 성별, 표정 예측 결과를 카메라 화면 상으로 출력합니다.

### Detect
나이, 성별, 표정 예측을 실행하거나 멈춥니다.  
Once 버튼으로 한 번만 실행할지, Continuous 버튼으로 연속으로 실행할지 정할 수 있습니다. 
또한, Stop 버튼을 통해 예측을 멈출 수 있습니다.

### Sensory Data
나이, 성별, 표정 예측 데이터 값을 출력합니다.

<br>  

# 블록
## 카메라 설정하기
나이, 성별, 표정 예측 모듈에 사용할 카메라를 선택합니다.

![](https://github.com/user-attachments/assets/d499d8cc-2a58-4253-940f-2f503b376d5f)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|camera|드롭다운 옵션|사용할 카메라|연결 가능한 카메라 리스트|

### 자바스크립트 코드
```javascript
// 특정 카메라를 나이, 성별, 표정 예측을 위한 카메라로 정하기 (id는 예시)
$('FaceExpression*0:camera.deviceId').d = '035658da47183882a695a82c45b8f3e9ae50cef47945ccdc3f31e1ae1fbca9cb';
```

### 파이썬 코드
```python
# 특정 카메라를 나이, 성별, 표정 예측을 위한 카메라로 정하기 (id는 예시)
__('FaceExpression*0:camera.deviceId').d = '035658da47183882a695a82c45b8f3e9ae50cef47945ccdc3f31e1ae1fbca9cb'
```

## 나이, 성별, 표정 모델 불러오기
학습된 나이, 성별, 표정 예측 모델을 불러옵니다.  
'나이, 성별, 표정' 모듈의 기능을 사용하기 위해서는 이 작업이 반드시 필요합니다.


![](https://github.com/user-attachments/assets/c98dabfb-3e63-439a-af5a-df3f8fda1eb7)


### 자바스크립트 코드
```javascript
// 나이, 성별, 표정 모델 불러오기 | 기다리기 O
$('FaceExpression*0:load_model').d = 1;
await $('FaceExpression*0:!load_model').w();

// 나이, 성별, 표정 모델 불러오기 | 기다리기 X
$('FaceExpression*0:load_model').d = 1;
```

### 파이썬 코드
```python
# 나이, 성별, 표정 모델 불러오기 | 기다리기 O
__('FaceExpression*0:load_model').d = 1
await __('FaceExpression*0:!load_model').w()

# 나이, 성별, 표정 모델 불러오기 | 기다리기 X
__('FaceExpression*0:load_model').d = 1
```

## 나이, 성별, 표정을 한 번 인식하기
현재 화면에 있는 얼굴을 분석하여 예측한 나이, 성별, 표정을 딱 한번 표시합니다.

![](https://github.com/user-attachments/assets/234252f3-26ad-48da-b4a9-4c2a26419d95)


### 자바스크립트 코드
```javascript
// 나이, 성별, 표정을 한 번 인식하기
$('FaceExpression*0:detect.once').d = 1;
```

### 파이썬 코드
```python
# 나이, 성별, 표정을 한 번 인식하기
__('FaceExpression*0:detect.once').d = 1
```

## 나이, 성별, 표정을 연속으로 인식하기
나이, 성별, 표정 연속적으로 예측하기를 시작하거나 중지합니다.  
연속으로 예측하기를 시작하면, 현재 화면에 있는 얼굴을 계속 따라가며 예측한 결과를 화면상에 표시합니다.

![](https://github.com/user-attachments/assets/47cf9e3e-f26f-4afe-900f-cd8d8e08b6c7)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|toggle|드롭다운 옵션|예측하기|시작하기(1), 중지하기(0)|

### 자바스크립트 코드
```javascript
// 연속으로 나이, 성별, 표정 예측 시작하기
$('FaceExpression*0:detect.continuous').d = 1;

// 연속으로 나이, 성별, 표정 예측 중지하기
$('FaceExpression*0:detect.continuous').d = 0;
```

### 파이썬 코드
```python
# 연속으로 나이, 성별, 표정 예측 시작하기
__('FaceExpression*0:detect.continuous').d = 1

# 연속으로 나이, 성별, 표정 예측 중지하기
__('FaceExpression*0:detect.continuous').d = 0
```

## 나이, 성별, 표정 찾기 결과 보이기
카메라 화면에 나이, 성별, 표정 예측 결과를 표시할지 말지를 결정합니다.

![](https://github.com/user-attachments/assets/9f2bacce-1b34-44eb-8952-c07dadac60ef)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|toggle|드롭다운 옵션|예측 결과|보이기(1), 숨기기(0)|

### 자바스크립트 코드
```javascript
// 나이, 성별, 표정 예측 결과 보이기
$('FaceExpression*0:display').d = 1;

// 나이, 성별, 표정 예측 결과 숨기기
$('FaceExpression*0:display').d = 0;
```

### 파이썬 코드
```python
# 나이, 성별, 표정 예측 결과 보이기
__('FaceExpression*0:display').d = 1

# 나이, 성별, 표정 예측 결과 숨기기
__('FaceExpression*0:display').d = 0
```

## 나이 예측 데이터
나이 예측 결과 데이터를 반환합니다.


![](https://github.com/user-attachments/assets/1546e580-6534-4c34-9059-c5b9ebfa5650)


### 자바스크립트 코드
```javascript
// 나이 예측 데이터
$('FaceExpression*0:age').d;
```

### 파이썬 코드
```python
# 나이 예측 데이터
__('FaceExpression*0:age').d
```

## 성별 예측 데이터
성별 예측 결과 데이터를 반환합니다.  
반환 데이터는 '남성', '여성' 입니다.

![](https://github.com/user-attachments/assets/797d06bc-ce26-41cb-aefe-bda163a7515f)


### 자바스크립트 코드
```javascript
// 성별 예측 데이터
$('FaceExpression*0:gender.name').d;
```

### 파이썬 코드
```python
# 성별 예측 데이터
__('FaceExpression*0:gender.name').d
```

## 표정 예측 데이터
표정 예측 결과 데이터를 반환합니다.  
반환 데이터는 '행복', '슬픔', '화남', '두려움', '혐오', '놀람', '무표정' 입니다.


![](https://github.com/user-attachments/assets/4a7edaba-f12a-4e72-8708-4eed55523e45)


### 자바스크립트 코드
```javascript
// 표정 예측 데이터
$('FaceExpression*0:expression.name').d;
```

### 파이썬 코드
```python
# 표정 예측 데이터
__('FaceExpression*0:expression.name').d
```

## 성별이 인식되었는가?
성별 예측 여부를 **참(1) / 거짓(0)** (으)로 반환합니다.


![](https://github.com/user-attachments/assets/8eeabf8c-5b00-4ac9-b3f2-41aed1e2a99d)


### 자바스크립트 코드
```javascript
// 성별이 인식되었는가?
$('FaceExpression*0:gender.detected').d;
```

### 파이썬 코드
```python
# 성별이 인식되었는가?
__('FaceExpression*0:gender.detected').d
```

## 성별이 ~ 인가?
선택한 성별과 예측 성별의 일치 여부에 따라 **참(1) / 거짓(0)** (으)로 반환합니다.


![](https://github.com/user-attachments/assets/d1551683-9aea-4424-8bf3-a7f6f119e387)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|gender|드롭다운 옵션|성별|남성(male), 여성(female)|

### 자바스크립트 코드
```javascript
// 예측 성별이 남성인가?
$('FaceExpression*0:gender.name').d === '남성';

// 예측 성별이 여성인가?
$('FaceExpression*0:gender.name').d === '여성';

```

### 파이썬 코드
```python
# 예측 성별이 남성인가?
__('FaceExpression*0:gender.name').d == '남성'

# 예측 성별이 여성인가?
__('FaceExpression*0:gender.name').d == '여성'
```

## 성별이 ~ 일 확률(신뢰도)
선택한 성별과 예측 성별의 일치 확률(신뢰도)을 반환합니다.  
반환값은 0 ~ 1 사이 실수 입니다.


![](https://github.com/user-attachments/assets/77070310-71c2-44df-a483-398728336980)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|gender|드롭다운 옵션|성별|남성(0), 여성(1)|

### 자바스크립트 코드
```javascript
// 예측 성별이 남성일 확률
$('FaceExpression*0:gender.confidence').d[0];

// 예측 성별이 여성일 확률
$('FaceExpression*0:gender.confidence').d[1];

```

### 파이썬 코드
```python
# 예측 성별이 남성일 확률
__('FaceExpression*0:gender.confidence').d[0]

# 예측 성별이 여성일 확률
__('FaceExpression*0:gender.confidence').d[1]
```

## 표정이 인식되었는가?
표정 인식 여부에 따라 **참(1) / 거짓(0)** (으)로 반환합니다.


![](https://github.com/user-attachments/assets/886f0d12-a6bf-46c8-89b2-fe509e8b4060)


### 자바스크립트 코드
```javascript
// 표정이 인식되었는가?
$('FaceExpression*0:expression.detected').d;

```

### 파이썬 코드
```python
# 표정이 인식되었는가?
__('FaceExpression*0:expression.detected').d
```

## 표정이 ~ 인가?
선택한 표정과 예측 표정의 일치 여부에 따라 **참(1) / 거짓(0)** (으)로 반환합니다.

![](https://github.com/user-attachments/assets/4c89d117-5eca-4226-b85d-c4d0b34cfae3)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|expression|드롭다운 옵션|표정|행복(happy), 슬픔(sad), 화남(angry), 두려움(afraid), 혐오(disgust), 놀람(surprise), 무표정(expressionless)|

### 자바스크립트 코드
```javascript
// 예측 표정이 행복인가?
$('FaceExpression*0:expression.name').d === '행복';

// 예측 표정이 슬픔인가?
$('FaceExpression*0:expression.name').d === '슬픔';

// 예측 표정이 화남인가?
$('FaceExpression*0:expression.name').d === '화남';

// 예측 표정이 두려움인가?
$('FaceExpression*0:expression.name').d === '두려움';

// 예측 표정이 혐오인가?
$('FaceExpression*0:expression.name').d === '혐오';

// 예측 표정이 놀람인가?
$('FaceExpression*0:expression.name').d === '놀람';

// 예측 표정이 무표정인가?
$('FaceExpression*0:expression.name').d === '무표정';
```

### 파이썬 코드
```python
# 예측 표정이 행복인가?
__('FaceExpression*0:expression.name').d == '행복'

# 예측 표정이 슬픔인가?
__('FaceExpression*0:expression.name').d == '슬픔'

# 예측 표정이 화남인가?
__('FaceExpression*0:expression.name').d == '화남'

# 예측 표정이 두려움인가?
__('FaceExpression*0:expression.name').d == '두려움'

# 예측 표정이 혐오인가?
__('FaceExpression*0:expression.name').d == '혐오'

# 예측 표정이 놀람인가?
__('FaceExpression*0:expression.name').d == '놀람'

# 예측 표정이 무표정인가?
__('FaceExpression*0:expression.name').d == '무표정'
```

## 표정이 ~ 일 확률(신뢰도)
선택한 표정과 예측 표정의 일치 확률(신뢰도)을 반환합니다.  
반환값은 0 ~ 1 사이 실수 입니다.


![](https://github.com/user-attachments/assets/2d956b7a-fbe6-4874-82b8-b23581b3e30a)


### 드롭다운 옵션 및 입력값
| 이름 | 구분 | 설명 | 범위 / 종류 |  
| ------ | ------ | ------ | ------ |
|expression|드롭다운 옵션|표정|행복(0), 슬픔(1), 화남(2), 두려움(3), 혐오(4), 놀람(5), 무표정(6)|

### 자바스크립트 코드
```javascript
// 예측 표정이 행복일 확률
$('FaceExpression*0:expression.confidence').d[0];

// 예측 표정이 슬픔일 확률
$('FaceExpression*0:expression.confidence').d[1];

// 예측 표정이 화남일 확률
$('FaceExpression*0:expression.confidence').d[2];

// 예측 표정이 두려움일 확률
$('FaceExpression*0:expression.confidence').d[3];

// 예측 표정이 혐오일 확률
$('FaceExpression*0:expression.confidence').d[4];

// 예측 표정이 놀람일 확률
$('FaceExpression*0:expression.confidence').d[5];

// 예측 표정이 무표정일 확률
$('FaceExpression*0:expression.confidence').d[6];

```

### 파이썬 코드
```python
# 예측 표정이 행복일 확률
__('FaceExpression*0:expression.confidence').d[0]

# 예측 표정이 슬픔일 확률
__('FaceExpression*0:expression.confidence').d[1]

# 예측 표정이 화남일 확률
__('FaceExpression*0:expression.confidence').d[2]

# 예측 표정이 두려움일 확률
__('FaceExpression*0:expression.confidence').d[3]

# 예측 표정이 혐오일 확률
__('FaceExpression*0:expression.confidence').d[4]

# 예측 표정이 놀람일 확률
__('FaceExpression*0:expression.confidence').d[5]

# 예측 표정이 무표정일 확률
__('FaceExpression*0:expression.confidence').d[6]
```

## 나이, 성별, 표정 모델 로딩 상태값
나이, 성별, 표정 모델 로딩 상태를 반환합니다.  
아직 불러오지 않았다면 0, 불러오는 중이면 1, 불러오기를 완료했다면 2를 반환합니다.


![](https://github.com/user-attachments/assets/eda043ea-a947-422e-9749-2177669374bb)


### 자바스크립트 코드
```javascript
// 나이, 성별, 표정 모델 로딩 상태 값
$('FaceExpression*0:model_state').d;
```

### 파이썬 코드
```python
# 나이, 성별, 표정 모델 로딩 상태 값
__('FaceExpression*0:model_state').d
```